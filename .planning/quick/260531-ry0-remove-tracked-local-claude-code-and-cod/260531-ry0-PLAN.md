---
status: in_progress
---

# Remove tracked local Claude Code and Codex settings

## Task

Stop tracking `.claude/settings.local.json` and `.codex/config.toml` while preserving
the local files, then add exact ignore rules for both paths.

## Verification

- Confirm both paths remain present locally.
- Confirm both paths are staged as deletions from the repository.
- Confirm Git ignores both local files.
