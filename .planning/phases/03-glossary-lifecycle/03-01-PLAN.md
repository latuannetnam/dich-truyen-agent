---
phase: glossary-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dich_truyen_agent/models.py
  - src/dich_truyen_agent/paths.py
  - src/dich_truyen_agent/workspace.py
  - src/dich_truyen_agent/glossary.py
  - src/dich_truyen_agent/cli.py
  - tests/test_glossary.py
autonomous: true
requirements:
  - GLOS-01
must_haves:
  truths:
    - "Pydantic models strictly validate the rich Structured Term Schema glossary and conflict report structures with extra='forbid' (GLOS-01, D-01)."
    - "Workspace paths correctly resolve book glossary.yaml, backup snapshots folder, and conflict reports under the book workspace root (D-01, D-04)."
    - "Workspace initialization and inspection automatically create and validate the glossary snapshots directory in the stage directories (D-04)."
    - "Automatic glossary generation uses the native agent model capability to extract and translate terms in context, saving them via a schema-validating atomic writer with is_canonical: false and source: initial_generation (GLOS-01, D-02)."
  artifacts:
    - path: "src/dich_truyen_agent/models.py"
      provides: "GlossaryTerm, BookGlossary, GlossaryConflict, and GlossaryConflictReport Pydantic schemas"
      contains: "class GlossaryTerm"
    - path: "src/dich_truyen_agent/paths.py"
      provides: "Glossary, snapshots, and conflict report paths on WorkspacePaths"
      contains: "self.glossary = "
    - path: "src/dich_truyen_agent/glossary.py"
      provides: "Atomic glossary schema writer and validation logic"
      contains: "def initialize_glossary_file"
    - path: "src/dich_truyen_agent/cli.py"
      provides: "Stubs and handlers for CLI glossary generation"
      contains: "generate-glossary"
  key_links:
    - from: "src/dich_truyen_agent/glossary.py"
      to: "src/dich_truyen_agent/storage.py"
      via: "atomic YAML serialization and load_yaml_model"
      pattern: "atomic_write_yaml"
    - from: "src/dich_truyen_agent/workspace.py"
      to: "src/dich_truyen_agent/paths.py"
      via: "retrieval of stage directories and glossary locations"
      pattern: "paths.glossary_snapshots"
---

<objective>
Build the initial glossary foundations: structured Pydantic models, workspace paths extension, automatic glossary schema writing validated by Pydantic models, and corresponding CLI stubs.

Purpose: Downstream translation workers and progressive merging must rely on a well-modeled initial glossary populated in literary context.
Output: Tested glossary data models, workspace paths integration, glossary writer helper, and initial CLI stubs.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-glossary-lifecycle/03-CONTEXT.md
@.planning/phases/03-glossary-lifecycle/03-RESEARCH.md
@.planning/phases/03-glossary-lifecycle/03-VALIDATION.md

<interfaces>
Create and use these contracts consistently:
- `GlossaryTerm(translation, category, source, is_canonical, note)`
- `BookGlossary(schema_version, terms)`
- `GlossaryConflict(term, existing_translation, existing_source, proposed_translation, proposed_source, chapter_id)`
- `GlossaryConflictReport(schema_version, conflicts)`
- `initialize_glossary_file(workspace_root, terms)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend models and paths for structured glossary and conflict records</name>
  <read_first>
    - `src/dich_truyen_agent/models.py`
    - `src/dich_truyen_agent/paths.py`
    - `src/dich_truyen_agent/workspace.py`
  </read_first>
  <files>
    src/dich_truyen_agent/models.py
    src/dich_truyen_agent/paths.py
    src/dich_truyen_agent/workspace.py
    tests/test_glossary.py
  </files>
  <behavior>
    - `GlossaryTerm` uses strict Pydantic model with fields validated appropriately.
    - `BookGlossary` contains a dictionary mapping Chinese terms to `GlossaryTerm` objects.
    - `WorkspacePaths` includes `glossary` (yaml), `glossary_snapshots` (dir), and `glossary_conflicts` (yaml).
    - `initialize_workspace` creates `glossary_snapshots` folder automatically.
  </behavior>
  <action>
    Add `GlossaryTerm`, `BookGlossary`, `GlossaryConflict`, and `GlossaryConflictReport` to `models.py`.
    Extend `WorkspacePaths` in `paths.py` to include:
    * `self.glossary = self.root / "glossary.yaml"`
    * `self.glossary_snapshots = self.checkpoints / "glossary-snapshots"`
    * `self.glossary_conflicts = self.reports / "glossary-conflicts.yaml"`
    Update `stage_directories` on `WorkspacePaths` to include `self.glossary_snapshots`.
    Extend `inspect_workspace` in `workspace.py` to optionally load `BookGlossary` and `GlossaryConflictReport` if they exist.
    Create `tests/test_glossary.py` and write unit tests verifying that all models strictly reject invalid keys, empty fields, and duplicate terms.
  </action>
  <verify>
    <automated>uv run pytest tests/test_glossary.py -q -k "models or paths"</automated>
  </verify>
  <acceptance_criteria>
    - Pydantic models for glossary and conflict report reject invalid properties.
    - Workspace directories initialized include the new glossary snapshots directory.
    - `uv run pytest tests/test_glossary.py -q -k "models or paths"` exits `0`.
  </acceptance_criteria>
  <done>Models and workspace paths successfully support the structured glossary schema and metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement initial glossary generation using native agent model capability</name>
  <read_first>
    - `src/dich_truyen_agent/models.py`
    - `src/dich_truyen_agent/paths.py`
    - `src/dich_truyen_agent/workspace.py`
    - `src/dich_truyen_agent/storage.py`
  </read_first>
  <files>
    src/dich_truyen_agent/glossary.py
    tests/test_glossary.py
  </files>
  <behavior>
    - Initial glossary generation uses the native agent model capability to read the first 3 raw chapters, extract terms, and translate/categorize them.
    - Python provides `initialize_glossary_file(workspace_root: Path, terms: dict[str, dict])` to strictly validate and write the BookGlossary to `glossary.yaml` atomically.
    - Terms successfully written receive `is_canonical=False` and `source="initial_generation"`.
  </behavior>
  <action>
    Create `src/dich_truyen_agent/glossary.py`.
    Implement `initialize_glossary_file(workspace_root: Path, terms: dict[str, dict]) -> OperationResult` that takes a dictionary of term mappings (e.g. Chinese -> {translation, category, note}) extracted by the native agent model, validates it against `BookGlossary`, assigns `is_canonical=False` and `source="initial_generation"` to each term, and writes `glossary.yaml` atomically under the book root.
    In the test suite `tests/test_glossary.py`, write unit tests that simulate this by passing sample extracted terms and verifying they are correctly parsed, validated, and saved.
  </action>
  <verify>
    <automated>uv run pytest tests/test_glossary.py -q -k "generation"</automated>
  </verify>
  <acceptance_criteria>
    - The initialization helper accepts term dictionaries, validates them against Pydantic models, and writes `glossary.yaml` atomically.
    - Terms written are stored with `is_canonical=False` and `source="initial_generation"`.
    - Malformed or invalid schema inputs are strictly rejected.
    - `uv run pytest tests/test_glossary.py -q -k "generation"` exits `0`.
  </acceptance_criteria>
  <done>Initial glossary schema writer successfully validates and saves native-agent-extracted terms atomically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CLI commands and stubs for glossary generation</name>
  <read_first>
    - `src/dich_truyen_agent/cli.py`
    - `src/dich_truyen_agent/glossary.py`
  </read_first>
  <files>
    src/dich_truyen_agent/cli.py
    tests/test_glossary.py
  </files>
  <behavior>
    - CLI exposes `generate-glossary` command.
    - Calls `generate_initial_glossary` and outputs a compact result file.
  </behavior>
  <action>
    Extend `cli.py` to add `generate-glossary` subcommand.
    `generate-glossary` accepts `--workspace` or matches books root and book slug, and takes an optional `--chapters` CSV argument (defaults to `1,2,3`).
    Integrate `generate_initial_glossary` into `run_command` in `cli.py`, persist result under `reports/results/generate-glossary.yaml`.
    Write unit tests in `tests/test_glossary.py` to execute the CLI parser and ensure it runs the stub appropriately.
  </action>
  <verify>
    <automated>uv run pytest tests/test_glossary.py -q -k "cli"</automated>
  </verify>
  <acceptance_criteria>
    - `dich-truyen-agent generate-glossary --workspace <path>` command parses correctly.
    - The CLI invokes the generator and prints/saves the compact OperationResult metadata.
    - `uv run pytest tests/test_glossary.py -q -k "cli"` exits `0`.
  </acceptance_criteria>
  <done>CLI entrypoints and results persistence successfully support glossary generation operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External Text -> LLM Extraction | Raw Chinese text is parsed and translated; output terms must be strictly validated. |
| Workspace path resolution | Ensured book slug and paths are kept within the books workspace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `glossary.py` file access | mitigate | Validate book slug and relative paths to prevent traversal during glossary generation; use `as_workspace_relative`. |
| T-03-02 | Tampering | glossary writes | mitigate | Write `glossary.yaml` atomically via `atomic_write_yaml` temp sibling replace to prevent corruption. |
</threat_model>

<verification>
Run `uv run pytest tests/test_glossary.py -q`.
Run `uv run ruff check src tests`.
</verification>

<success_criteria>
- Structured glossary schemas are defined and validated by Pydantic models.
- Glossary generation parses raw chapter bodies, runs the scan, and writes the initial file.
- CLI command `generate-glossary` successfully triggers the workflow and records the result.
</success_criteria>

<output>
Create `.planning/phases/03-glossary-lifecycle/03-01-SUMMARY.md` when done.
</output>
