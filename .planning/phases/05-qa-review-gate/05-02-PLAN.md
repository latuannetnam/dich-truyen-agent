---
phase: qa-review-gate
plan: "02"
type: execute
wave: 2
depends_on: [05-01-PLAN.md]
files_modified:
  - src/dich_truyen_agent/cli.py
  - .agent/skills/check-translation/SKILL.md
  - tests/test_qa.py
autonomous: true
requirements:
  - QUAL-05
must_haves:
  truths:
    - "CLI command check-translation successfully writes the structured QA report to reports/qa-report.yaml and prints a formatted Markdown table to the console (QUAL-01, QUAL-02, QUAL-03)."
    - "CLI command approve-qa strictly ensures QA checks are current, warns/blocks if critical errors exist, and generates checkpoints/qa-approved.yaml (QUAL-05)."
    - "Checkpoint qa-approved.yaml contains evidence hashes of all translated files to prevent tampering downstream (QUAL-05)."
    - "User-facing check-translation skill is fully implemented with accurate CLI commands and verification steps (SKIL-01, QUAL-01)."
  artifacts:
    - path: "src/dich_truyen_agent/cli.py"
      provides: "CLI subcommands check-translation and approve-qa"
      contains: "check-translation"
    - path: ".agent/skills/check-translation/SKILL.md"
      provides: "The fully documented user-facing check-translation skill"
      contains: "$check-translation"
    - path: "tests/test_qa.py"
      provides: "E2E integration tests for CLI commands and checkpoint approval"
      contains: "def test_cli_check_and_approve_qa"
  key_links:
    - from: "src/dich_truyen_agent/cli.py"
      to: "src/dich_truyen_agent/qa.py"
      via: "invoking QA scanning logic"
      pattern: "run_qa_check"
    - from: "src/dich_truyen_agent/cli.py"
      to: "src/dich_truyen_agent/checkpoints.py"
      via: "registering QA gate checkpoints"
      pattern: "approve_checkpoint"
---

<objective>
Expose quality checks to the CLI and implement the user-facing check-translation skill.
This enables running full verification scans via command-line, producing beautiful terminal results, and securing the qa-approved checkpoint to validate downstream ebook conversions.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-qa-review-gate/05-CONTEXT.md
@.planning/phases/05-qa-review-gate/05-RESEARCH.md
@.planning/phases/05-qa-review-gate/05-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI subcommands check-translation and approve-qa</name>
  <read_first>
    - `src/dich_truyen_agent/cli.py`
    - `src/dich_truyen_agent/qa.py`
    - `src/dich_truyen_agent/checkpoints.py`
  </read_first>
  <files>
    src/dich_truyen_agent/cli.py
    tests/test_qa.py
  </files>
  <behavior>
    - Adds `check-translation` command: calls `run_qa_check()`, saves to `reports/qa-report.yaml`, and prints a beautiful Markdown findings table.
    - Adds `approve-qa` command: warns if QA findings exist, and generates `checkpoints/qa-approved.yaml` with correct hashes.
  </behavior>
  <action>
    Modify `src/dich_truyen_agent/cli.py`.
    Add `check-translation` parser action:
    - Runs the QA scanner.
    - Writes the report YAML atomically.
    - Formats and displays a clean Markdown table with headers: Category, Severity, Chapter, Message.
    Add `approve-qa` parser action:
    - Verifies that `reports/qa-report.yaml` is current.
    - Prompts user or warns if blocking findings exist.
    - Calls `approve_checkpoint` using `CheckpointType.QA_APPROVED`, path of report, and all translated chapter file paths.
    Update `tests/test_qa.py` with CLI command execution integration tests using standard CLI parser test patterns.
  </action>
  <verify>
    <automated>uv run pytest tests/test_qa.py -q -k "cli"</automated>
  </verify>
  <acceptance_criteria>
    - Subcommands execute correctly via python entry point.
    - YAML report is persisted, and terminal outputs clean tables.
    - `approve-qa` creates checkpoint YAML with appropriate translated file hashes.
    - `uv run pytest tests/test_qa.py -q -k "cli"` exits `0`.
  </acceptance_criteria>
  <done>Subcommands check-translation and approve-qa are fully implemented and verified via CLI tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the check-translation skill file</name>
  <read_first>
    - `.agent/skills/check-translation/SKILL.md`
  </read_first>
  <files>
    .agent/skills/check-translation/SKILL.md
  </files>
  <behavior>
    - Replaces Phase 1 placeholder stub with the fully documented quality gate process.
    - Outlines step-by-step instructions for running the validation engine, interpreting console output, resolving conflicts, and creating checkpoints.
  </behavior>
  <action>
    Overwrite `.agent/skills/check-translation/SKILL.md` with complete skill documentation.
    Include details on using `main.py check-translation` and `main.py approve-qa`, and provide clear checklists for:
    - Chinese Residue Warnings
    - Abnormal Chapter Lengths
    - Unresolved glossary conflict resolutions.
  </action>
  <verify>
    <automated>uv run ruff check .agent/skills/check-translation/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - The skill documentation matches standard Antigravity skill structure and conventions.
    - Placeholder warning is removed and replaced with active validation workflow.
  </acceptance_criteria>
  <done>The user-facing check-translation skill is fully documented and prepared for integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
* Ensure `approve-qa` only accepts evidence paths that reside strictly within the book workspace directories to block traversal.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | Checkpoint verification | mitigate | Hashes of all translation files in the checkpoint prevent malicious or accidental downstream changes before exporting. |
</threat_model>

<verification>
Run `uv run pytest tests/test_qa.py -q`.
Run `uv run ruff check src tests`.
</verification>

<success_criteria>
- Terminal displays beautiful and clean Markdown reports summarizing quality verification status.
- `approve-qa` produces the cryptographically validated `qa-approved.yaml` checkpoint file.
</success_criteria>

<output>
Create `.planning/phases/05-qa-review-gate/05-02-SUMMARY.md` when done.
</output>
