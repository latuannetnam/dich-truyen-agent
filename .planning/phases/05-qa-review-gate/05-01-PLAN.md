---
phase: qa-review-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dich_truyen_agent/models.py
  - src/dich_truyen_agent/qa.py
  - tests/test_qa.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
must_haves:
  truths:
    - "QA checks are strictly read-only and do not write to or mutate any translation or raw source files (QUAL-04, D-01)."
    - "Structural checks correctly diagnose missing, empty, and out-of-order chapter translations based on catalog entries (QUAL-02, D-02)."
    - "Incompleteness checking accurately flags mismatched quotation marks and bracket pairs, as well as sentences missing ending terminal punctuation (QUAL-02, D-02)."
    - "Chinese residue scanning flags all CJK ideographs and punctuation marks along with exact lines, columns, and 40-character snippets (QUAL-03, D-03)."
    - "Abnormal chapter length validation flags chapters where the Vietnamese-to-Chinese character count ratio is < 0.6 or > 2.0 (QUAL-03, D-04)."
    - "Glossary conflict checks extract and report unresolved terms logged in the conflict report (QUAL-03, D-05)."
  artifacts:
    - path: "src/dich_truyen_agent/qa.py"
      provides: "Core quality validation engine and heuristics"
      contains: "def run_qa_check"
    - path: "src/dich_truyen_agent/models.py"
      provides: "Structured QA models and schemas"
      contains: "class QAReport"
    - path: "tests/test_qa.py"
      provides: "Isolated unit tests covering all deterministic QA validation checks"
      contains: "def test_run_qa_check_all_rules"
  key_links:
    - from: "src/dich_truyen_agent/qa.py"
      to: "src/dich_truyen_agent/models.py"
      via: "returning structured QA findings and reports"
      pattern: "QAReport"
---

<objective>
Build the core QA quality checks engine and domain models.
This implements the deterministic structural integrity, residue, character length ratio, and glossary conflict checks that evaluate book translations in a strictly non-mutating manner.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-qa-review-gate/05-CONTEXT.md
@.planning/phases/05-qa-review-gate/05-RESEARCH.md

<interfaces>
- `run_qa_check(workspace_root: Path) -> QAReport`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structured QA models to models.py</name>
  <read_first>
    - `src/dich_truyen_agent/models.py`
  </read_first>
  <files>
    src/dich_truyen_agent/models.py
  </files>
  <behavior>
    - Defines QAFindingType, QAFinding, and QAReport Pydantic models.
    - Ensures validations and assignments are strict.
  </behavior>
  <action>
    Add the `QAFindingType` string enum (values: `structural`, `residue`, `length`, `glossary`).
    Add the `QAFinding` Pydantic model with fields: `chapter_id` (int), `finding_type` (QAFindingType), `severity` (str, e.g., 'warning'/'error'), `message` (str), and `details` (dict).
    Add the `QAReport` Pydantic model with fields: `schema_version` (int = 1), `generated_at` (datetime), `summary` (dict containing counts), and `findings` (list[QAFinding]).
  </action>
  <verify>
    <automated>uv run python -c "from dich_truyen_agent.models import QAReport; print(QAReport.model_fields)"</automated>
  </verify>
  <acceptance_criteria>
    - Pydantic models compile and are importable.
    - Fields accurately capture all metadata of a QA finding.
  </acceptance_criteria>
  <done>QA data models are successfully added to models.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement core QA checks under qa.py</name>
  <read_first>
    - `src/dich_truyen_agent/qa.py`
    - `src/dich_truyen_agent/paths.py`
    - `src/dich_truyen_agent/storage.py`
  </read_first>
  <files>
    src/dich_truyen_agent/qa.py
    tests/test_qa.py
  </files>
  <behavior>
    - Performs non-mutating validation over catalog, raw text, and translation files.
    - Structural checks: identify missing, empty, and out-of-order chapters.
    - Incompleteness heuristics: unbalanced quote pairs and missing ending terminal punctuation.
    - Chinese residue check: detect CJK ideographs/punctuation and capture exact location + context snippet.
    - Abnormal length: Vietnamese-to-Chinese character ratio bounds (< 0.6 or > 2.0).
    - Unresolved conflicts: pull conflicts from glossary-conflicts.yaml.
  </behavior>
  <action>
    Create `src/dich_truyen_agent/qa.py`.
    Implement `run_qa_check(workspace_root: Path) -> QAReport`.
    - Load catalog and state. Check each catalog entry for missing/empty/incomplete files.
    - Check terminal character endings (`.`, `?`, `!`, `”`, `」`, `...`).
    - Match unbalanced quotes (`"`, `「` vs `」`, `『` vs `』`, `(` vs `)`).
    - Match CJK Unified Ideographs (`\u4e00-\u9fff`) and CJK symbols/punctuation (`\u3000-\u303f`: `。`, `，`, `、`, `「`, `」`, `『`, `』`) using regex. Record line, column, and 20-character surrounding context.
    - Compute and validate character length ratios (Vietnamese length / Chinese length).
    - Parse conflicts from `reports/glossary-conflicts.yaml` if the file exists.
    Create `tests/test_qa.py` and write exhaustive unit tests for each validation check using dummy files and custom error patterns.
  </action>
  <verify>
    <automated>uv run pytest tests/test_qa.py -q</automated>
  </verify>
  <acceptance_criteria>
    - QA validations execute successfully without changing any translation or raw files.
    - All five core quality checks (structural, completeness, residue, length ratio, glossary conflicts) are verified and covered by tests.
    - `uv run pytest tests/test_qa.py` exits `0`.
  </acceptance_criteria>
  <done>QA validation checker and unit tests are fully completed and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
* Read-only guarantees: Verify that no file system write tools are called within `qa.py` checks.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Denial of Service | Chinese residue scan | mitigate | Optimize regex matching using compiled `re` expressions to prevent performance degradation on long texts. |
</threat_model>

<verification>
Run `uv run pytest tests/test_qa.py -q`.
Run `uv run ruff check src tests`.
</verification>

<success_criteria>
- Core quality scanning runs successfully and generates robust `QAReport` payloads.
- Structural, incomplete, residue, length ratio, and glossary conflict errors/warnings are correctly classified and logged.
</success_criteria>

<output>
Create `.planning/phases/05-qa-review-gate/05-01-SUMMARY.md` when done.
</output>
