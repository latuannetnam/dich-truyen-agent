---
phase: sequential-agent-native-translation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/dich_truyen_agent/cli.py
  - src/dich_truyen_agent/workspace.py
  - tests/test_translation.py
autonomous: true
requirements:
  - TRAN-05
  - TRAN-06
  - TRAN-07
must_haves:
  truths:
    - "CLI command show-translation-progress inspects BookState and outputs the exact next pending chapter, overall count completed, and overall queue status (TRAN-05, D-03)."
    - "Orchestration progress checker blocks progress if any preceding chapter's translation is incomplete, enforcing strict ordering (TRAN-05)."
  artifacts:
    - path: "src/dich_truyen_agent/workspace.py"
      provides: "Translation progress and ordering validation helpers"
      contains: "def get_next_pending_translation"
    - path: "src/dich_truyen_agent/cli.py"
      provides: "CLI subcommand to inspect translation status queue"
      contains: "show-translation-progress"
  key_links:
    - from: "src/dich_truyen_agent/workspace.py"
      to: "src/dich_truyen_agent/models.py"
      via: "BookState parsing and chapter stage inspections"
      pattern: "StageStatus"
---

<objective>
Implement the helper support for sequential orchestration, ordering verification, and status querying.
This equips the `$translate-book` skill orchestrator with precise commands to drive the loop, maintain strict chapter order, and handle safe resume checkpoints.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-sequential-agent-native-translation/04-CONTEXT.md
@.planning/phases/04-sequential-agent-native-translation/04-RESEARCH.md
@.planning/phases/04-sequential-agent-native-translation/04-01-PLAN.md

<interfaces>
- `get_next_pending_translation(workspace_root: Path) -> OperationResult`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sequential ordering validation and next pending chapter finder</name>
  <read_first>
    - `src/dich_truyen_agent/workspace.py`
    - `src/dich_truyen_agent/models.py`
  </read_first>
  <files>
    src/dich_truyen_agent/workspace.py
    tests/test_translation.py
  </files>
  <behavior>
    - Scans state chapters in sequential order of their catalog indexes.
    - Identifies the first chapter with `translation` stage in `PENDING` state.
    - Verifies that all chapters before it are completely `COMPLETED` in their translation.
    - If there's any gap (e.g. out of order completion), blocks and reports the ordering gap.
  </behavior>
  <action>
    Implement `get_next_pending_translation(workspace_root: Path) -> OperationResult` in `workspace.py`.
    It loads catalog and state, iterates sequentially, and finds the first pending chapter. It checks that all chapters before it are completed. If yes, it returns success with next chapter details (ID, slug, raw title) in the progress summary and result payload. If a preceding chapter is incomplete, it returns blocked status.
    Write unit tests in `tests/test_translation.py` verifying correct next-chapter identification, gap blocking, and completed states.
  </action>
  <verify>
    <automated>uv run pytest tests/test_translation.py -q -k "pending"</automated>
  </verify>
  <acceptance_criteria>
    - Finder correctly identifies the next sequential chapter to translate.
    - Out-of-order prior chapters trigger a blocked status, preventing gap translations.
    - `uv run pytest tests/test_translation.py -q -k "pending"` exits `0`.
  </acceptance_criteria>
  <done>Ordering validation and next pending chapter finder successfully enforce strict continuity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI command for translation progress and queue inspections</name>
  <read_first>
    - `src/dich_truyen_agent/cli.py`
    - `src/dich_truyen_agent/workspace.py`
  </read_first>
  <files>
    src/dich_truyen_agent/cli.py
    tests/test_translation.py
  </files>
  <behavior>
    - CLI command `show-translation-progress` prints the sequential queue status.
    - Reports next target chapter ID, completed count, and total count.
  </behavior>
  <action>
    Add subcommand `show-translation-progress` to `cli.py` parsing.
    Integrate it into `run_command` to invoke `get_next_pending_translation()`, printing and persisting results under `reports/results/`.
    Write unit tests in `tests/test_translation.py` running the CLI subcommand on dummy workspaces with different states.
  </action>
  <verify>
    <automated>uv run pytest tests/test_translation.py -q -k "progress_cli"</automated>
  </verify>
  <acceptance_criteria>
    - CLI `show-translation-progress` command outputs clean queue metrics and next targets.
    - Results successfully recorded to reports folder.
    - `uv run pytest tests/test_translation.py -q -k "progress_cli"` exits `0`.
  </acceptance_criteria>
  <done>CLI subcommand successfully exposes translation queue status for orchestration loop consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
* Read-only inspection of progress prevents tampering.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Denial of Service | Ordering gaps | mitigate | Block next translation if a prior chapter is incomplete, preventing messy resume gaps. |
</threat_model>

<verification>
Run `uv run pytest tests/test_translation.py -q`.
Run `uv run ruff check src tests`.
</verification>

<success_criteria>
- `show-translation-progress` cleanly returns the next sequential target and blocks if any prior chapter gap exists.
</success_criteria>

<output>
Create `.planning/phases/04-sequential-agent-native-translation/04-02-SUMMARY.md` when done.
</output>
