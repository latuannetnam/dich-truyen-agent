---
phase: sequential-agent-native-translation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - .agent/skills/translate-book/SKILL.md
  - tests/test_translation.py
autonomous: true
requirements:
  - SKIL-01
  - TRAN-01
  - TRAN-02
  - TRAN-03
  - TRAN-04
  - TRAN-05
  - TRAN-06
  - TRAN-07
must_haves:
  truths:
    - "The translate-book skill is written end-to-end as a fully documented Antigravity skill utilizing subagent invocations, sequential orchestration, retries, and resume behaviors (SKIL-01, TRAN-02, TRAN-05, TRAN-06, TRAN-07)."
    - "Subagent prompt contains the complete Xianxia-specialized prompt, continuity variables (Chapter N-1), style directives, and progressive dictionary mapping instructions (TRAN-02, TRAN-03)."
    - "Integration fixtures and tests verify the complete multi-command CLI lifecycle on a mock sequential crawl-approved workspace (TRAN-07)."
  artifacts:
    - path: ".agent/skills/translate-book/SKILL.md"
      provides: "Sequential translation Antigravity skill interface"
      contains: "invoke_subagent"
    - path: "tests/test_translation.py"
      provides: "End-to-end integration test validating the workflow CLI command sequence"
      contains: "def test_e2e_translation_workflow"
---

<objective>
Deliver the user-facing sequential translation skill `$translate-book` and integration fixtures.
This completes Phase 4, giving the user a fully functional, resilient, and resume-safe sequential translation capability.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-sequential-agent-native-translation/04-CONTEXT.md
@.planning/phases/04-sequential-agent-native-translation/04-RESEARCH.md
@.planning/phases/04-sequential-agent-native-translation/04-01-PLAN.md
@.planning/phases/04-sequential-agent-native-translation/04-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the end-to-end sequential translation skill markdown</name>
  <read_first>
    - `.agent/skills/translate-book/SKILL.md`
    - `../dich-truyen-tien-hiep/.agents/skills/translate-error-chapters/SKILL.md`
  </read_first>
  <files>
    .agent/skills/translate-book/SKILL.md
  </files>
  <behavior>
    - Defines the `$translate-book` skill to enforce crawl-approved checkpoints.
    - Loops sequentially through chapters by running `show-translation-progress` to find next pending targets.
    - Resolves absolute paths via `prepare-translation-context`.
    - Spawns isolated translation subagents using clean prompt guidelines.
    - Promotes translations atomically using `promote-chapter`.
    - Integrates 3-retry limits with polite backoffs, stopping on exhaustion for safe resumption.
  </behavior>
  <action>
    Modify `.agent/skills/translate-book/SKILL.md`.
    Write the complete orchestrator workflow logic and subagent execution protocol instructions in clean Markdown format, registering the core behaviors, command triggers, prompt payload templates, sandbox cleanliness rules, and JSON output formatting.
  </action>
  <verify>
    <automated>uv run ruff check .agent/skills/translate-book/SKILL.md --ignore=E999</automated>
  </verify>
  <acceptance_criteria>
    - The skill file has no TBDs/placeholders.
    - Outlines exact subagent prompt parameters and CLI command inputs.
  </acceptance_criteria>
  <done>Sequential translation skill markdown is fully authored and registered in the workspace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement end-to-end integration tests for the translation CLI sequence</name>
  <read_first>
    - `tests/test_translation.py`
    - `src/dich_truyen_agent/workspace.py`
    - `src/dich_truyen_agent/cli.py`
  </read_first>
  <files>
    tests/test_translation.py
  </files>
  <behavior>
    - Executes the sequence: check-gate -> progress -> prepare-context -> mock translate -> promote -> verify progress.
    - Verifies transition of multiple chapters sequentially.
    - Verifies that resume picks up at the first uncompleted chapter correctly.
  </behavior>
  <action>
    Add integration test `test_e2e_translation_workflow(books_root, metadata, catalog, style)` to `tests/test_translation.py`.
    It initializes a workspace, simulates a crawl-approved checkpoint, and runs a loop through three chapters where it:
    1. Runs progress checker to identify next target.
    2. Runs context preparation to get absolute paths.
    3. Simulates the translator subagent by writing staged files and proposals.
    4. Runs `promote-chapter` to commit.
    5. Verifies progress moves sequentially and hashes/states match catalog predictions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_translation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Full end-to-end CLI workflow executes successfully.
    - All tests pass, and codebase linting contains no warnings.
    - `uv run pytest tests/test_translation.py -q` exits `0`.
  </acceptance_criteria>
  <done>E2E integration tests fully validate the CLI workflow lifecycle and resume behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
* Subagent isolation preserves orchestrator context window size.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Output pollution | mitigate | Subagent prompts enforce Sino-Vietnamese Xianxia genre conventions and clean modern/English conjunction leakages. |
</threat_model>

<verification>
Run `uv run pytest tests/test_translation.py -q`.
Run `uv run ruff check src tests`.
</verification>

<success_criteria>
- The user-facing `$translate-book` skill uses clean isolated subagent invocations.
- The CLI workflow sequence operates flawlessly across multiple sequential chapters in integration tests.
</success_criteria>

<output>
Create `.planning/phases/04-sequential-agent-native-translation/04-03-SUMMARY.md` when done.
</output>
