---
phase: sequential-agent-native-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dich_truyen_agent/cli.py
  - src/dich_truyen_agent/workspace.py
  - tests/test_translation.py
autonomous: true
requirements:
  - TRAN-01
  - TRAN-02
  - TRAN-03
  - TRAN-04
must_haves:
  truths:
    - "CLI command prepare-translation-context returns absolute paths for raw chapter, style, glossary, and predecessor chapter translations, enforcing crawl approval gates (TRAN-01, TRAN-03, D-01)."
    - "Context builder applies predecessor translation fallback, returning null for previous context if N-1 does not exist, enabling clean start/resets (D-04)."
    - "Context builder blocks if crawl-approved gate fails or is missing (TRAN-01)."
    - "CLI command promote-chapter strictly validates staging existence, minimum character lengths, and proposals YAML syntax (TRAN-04, D-02)."
    - "Promotion copies staging text atomically to translations, merges glossary proposals progressive Snapshots, updates state.yaml to COMPLETED, and cleans staging files (TRAN-04, D-02)."
  artifacts:
    - path: "src/dich_truyen_agent/workspace.py"
      provides: "Context preparation and staging promotion helpers"
      contains: "def prepare_translation_context"
    - path: "src/dich_truyen_agent/cli.py"
      provides: "CLI subcommands for context preparation and promotion"
      contains: "prepare-translation-context"
    - path: "tests/test_translation.py"
      provides: "Unit tests covering context resolution, gate checks, and atomic promotions"
      contains: "def test_prepare_context_blocks_without_gate"
  key_links:
    - from: "src/dich_truyen_agent/workspace.py"
      to: "src/dich_truyen_agent/checkpoints.py"
      via: "crawl checkpoint gate checking"
      pattern: "check_gate"
    - from: "src/dich_truyen_agent/workspace.py"
      to: "src/dich_truyen_agent/glossary.py"
      via: "chapter progressive proposal merging"
      pattern: "merge_glossary_proposals"
---

<objective>
Build the translation worker primitives: the context preparation helper and the staging validation/promotion engine.
This lays the foundation for isolated, safe chapter translations before sequential loop orchestration is built.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-sequential-agent-native-translation/04-CONTEXT.md
@.planning/phases/04-sequential-agent-native-translation/04-RESEARCH.md

<interfaces>
- `prepare_translation_context(workspace_root: Path, chapter_id: int) -> OperationResult`
- `promote_chapter_translation(workspace_root: Path, chapter_id: int) -> OperationResult`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement context preparation with gate checks and predecessor fallback</name>
  <read_first>
    - `src/dich_truyen_agent/workspace.py`
    - `src/dich_truyen_agent/checkpoints.py`
    - `src/dich_truyen_agent/paths.py`
  </read_first>
  <files>
    src/dich_truyen_agent/workspace.py
    tests/test_translation.py
  </files>
  <behavior>
    - Enforces that `CheckpointType.CRAWL_APPROVED` exists and is current.
    - Resolves absolute paths for raw text, style, glossary, and Chapter N-1 translation context.
    - If Chapter N-1 is missing or N == 1, returns a fallback with null predecessor context.
  </behavior>
  <action>
    Implement `prepare_translation_context(workspace_root: Path, chapter_id: int) -> OperationResult` in `workspace.py`.
    It checks crawl approval gates, loads catalog/state, ensures the requested chapter is valid, and returns a JSON payload in its `reason` or dedicated field containing key absolute paths: `raw_path`, `style_path`, `glossary_path`, `prev_translation_path` (using fallback logic if `chapter_id == 1` or file is missing).
    Write unit tests in `tests/test_translation.py` verifying successful context resolution, fallback behavior, and blocking when crawl checkpoint is absent or stale.
  </action>
  <verify>
    <automated>uv run pytest tests/test_translation.py -q -k "context"</automated>
  </verify>
  <acceptance_criteria>
    - Context builder enforces `crawl-approved` checkpoint.
    - Predecessor translation fallback correctly handles Chapter 1 or missing files by returning null and note.
    - `uv run pytest tests/test_translation.py -q -k "context"` exits `0`.
  </acceptance_criteria>
  <done>Context preparation logic is fully validated and supports predecessor fallbacks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement staging validation and atomic promotion helper</name>
  <read_first>
    - `src/dich_truyen_agent/workspace.py`
    - `src/dich_truyen_agent/glossary.py`
    - `src/dich_truyen_agent/storage.py`
  </read_first>
  <files>
    src/dich_truyen_agent/workspace.py
    tests/test_translation.py
  </files>
  <behavior>
    - Validates that `staging/chuong-N-staged.txt` exists, is non-empty, and has reasonable size.
    - Validates glossary proposals YAML file format in staging.
    - Atomically promotes translation, progressive merges glossary, updates `state.yaml` translation stage, and cleans up staging.
  </behavior>
  <action>
    Implement `promote_chapter_translation(workspace_root: Path, chapter_id: int) -> OperationResult` in `workspace.py`.
    Add validations for existence and non-emptiness. Atomically replace/write to `translations/` path resolved from the catalog, run proposal merging, compute sha256, update state, and unlink staging files.
    Write unit tests in `tests/test_translation.py` executing full promotion workflows on dummy staged files, verifying correct file paths, hash checksum storage, and cleanup.
  </action>
  <verify>
    <automated>uv run pytest tests/test_translation.py -q -k "promotion"</automated>
  </verify>
  <acceptance_criteria>
    - Promotion checks staging file size/integrity and rejects malformed inputs.
    - Output translation is written atomically, glossary proposals merged, state marked completed, and staging cleaned.
    - `uv run pytest tests/test_translation.py -q -k "promotion"` exits `0`.
  </acceptance_criteria>
  <done>Atomic promotion helper successfully secures translations, merges glossaries, and manages state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CLI commands for context preparation and staging promotion</name>
  <read_first>
    - `src/dich_truyen_agent/cli.py`
    - `src/dich_truyen_agent/workspace.py`
  </read_first>
  <files>
    src/dich_truyen_agent/cli.py
    tests/test_translation.py
  </files>
  <behavior>
    - CLI exposes `prepare-translation-context` and `promote-chapter`.
    - Outputs standard OperationResult console summaries.
  </behavior>
  <action>
    Add subcommands `prepare-translation-context` and `promote-chapter` to the parser in `cli.py`.
    Integrate them into `run_command` to invoke the corresponding workspace helpers and persist the results in `reports/results/`.
    Write unit tests in `tests/test_translation.py` running the CLI commands via `run_command` on test workspaces.
  </action>
  <verify>
    <automated>uv run pytest tests/test_translation.py -q -k "cli"</automated>
  </verify>
  <acceptance_criteria>
    - CLI subcommands parse arguments correctly and execute operations.
    - Results are recorded under results reports directory.
    - `uv run pytest tests/test_translation.py -q -k "cli"` exits `0`.
  </acceptance_criteria>
  <done>CLI subcommands successfully expose context preparation and promotion primitives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
* Staged translation text validation before promotion prevents raw-corruption.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Staging injection | mitigate | Enforce `promote-chapter` validates that the destination file path is strictly within the `translations` folder resolved from the immutable catalog (escapes blocked). |
| T-04-01-02 | Tampering | Staging cleanup | mitigate | Clean up staged files immediately following promotion to prevent residual file tampering or duplicate merge calls. |
</threat_model>

<verification>
Run `uv run pytest tests/test_translation.py -q`.
Run `uv run ruff check src tests`.
</verification>

<success_criteria>
- `prepare-translation-context` command resolves clean absolute paths, blocks on crawl approval, and handles Chapter 1 continuity fallbacks.
- `promote-chapter` command validates staged outputs, writes atomically, merges glossary, and updates `state.yaml` smoothly.
</success_criteria>

<output>
Create `.planning/phases/04-sequential-agent-native-translation/04-01-SUMMARY.md` when done.
</output>
